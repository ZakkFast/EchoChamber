{{#each this}}
<div class="comment-container row">
  <div class="col s10">
    <p class="left-align">Comment by {{user.user_name}} on {{format_date created_at}}</p>
    <p class="left-align">{{comment_text}}</p>

    </div>
  </div>
</div>
{{/each}}